<h1><b><i><%= @band.name %></i></b></h1>

<ul>
  <% @band.albums.each do |album| %>
    <li><a href="<%= album_url(album.id) %>"><%= album.name %></a></li>
  <% end %>
</ul>

<hr>

<a href="<%= new_band_album_url(@band.id) %>">Add An Album</a>

<!-- destroy button, edit button-->